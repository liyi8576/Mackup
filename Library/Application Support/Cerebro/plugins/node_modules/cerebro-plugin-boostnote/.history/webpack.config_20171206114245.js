
const path = require('path');

const defaultConfig = path.join('node_modules', 'cerebro-scriprts', 'config', 'webpack.config.js');

return function (config, env) {
  const c = defaultConfig(config, env);
  // customize as you want

  return c;
}
