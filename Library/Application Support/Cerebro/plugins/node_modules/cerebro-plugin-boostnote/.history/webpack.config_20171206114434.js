
const path = require('path');

const defaultConfig = require('node_modules/cerebro-scripts/config/webpack.config.js');

module.exports = function (config, env) {
  const c = defaultConfig(config, env);
  // customize as you want

  return c;
}
